import React from "react";
import ReactDOM from "react-dom/client";
import MarketPriceApp from "./MarketPriceApp.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MarketPriceApp />
  </React.StrictMode>
);
